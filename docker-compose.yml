version: "3.4"
services:
  database:
    image: mysql:latest
    ports:
      - "3306:3306"
    environment:
      - "MYSQL_ROOT_PASSWORD=root"
      - "MYSQL_DATABASE=rezeptApp"
      - "allowPublicKeyRetrieval=true"
      - "useSSL=false"
